# 定制版使用说明

> 本文档面向这份复刻（Fork）的使用者，假设你没有任何编程经验。
> 官方项目：https://github.com/yzr278892/arxiv-daily-researcher
> 本定制版基于官方 **v4.2**（2026-08-30）。

---

## 1. 这是什么

这是官方项目的**个人定制版**。定制内容只有一项：

**报告目录结构可以切换** —— 除了官方默认的「按数据源分目录」，新增了「按运行时间分目录」模式：一次运行生成的全部报告（所有数据源）放进同一个以运行时间命名的文件夹里，找"某天早上跑了什么"一目了然。

除此之外与官方 4.2 完全一致（含 4.1/4.2 的全部新功能：新版管理面板、逐项关键词权重、runtime 配置隔离等）。

---

## 2. 两个分支（branch）是什么关系

| 分支 | 内容 | 用途 |
|------|------|------|
| `main` | 官方 4.2 原版，原封不动 | 与上游保持一致的干净基线 |
| `custom` | 原版 + 定制提交 | **日常使用这个分支** |

---

## 3. 怎么切换报告目录布局

**方法一：网页面板（推荐）**

1. 打开新版管理面板 → 「数据源」页
2. 在「额外数据源」区找到「报告目录结构」下拉框（注意：只有启用了 PRL 或任一额外期刊来源时才会显示）
3. 选「按运行时间分目录」→ 保存

**方法二：改配置文件**

4.2 起主配置位于 `runtime/config.json`（首次启动会自动从旧的 `configs/config.json` 迁移）。找到 `report_settings` 段：

```jsonc
"report_settings": {
    "enable_html_report": true,
    "enable_markdown_report": true,
    "directory_structure": "date_grouped"   // by_source = 按数据源（默认）；date_grouped = 按运行时间
},
```

改完下次运行即生效。

---

## 4. 两种布局的文件夹长什么样

**按数据源（by_source，官方默认）**——每个数据源一个文件夹，文件名带时间戳：

```text
data/reports/daily_research/html/
├── arxiv/
│   ├── ARXIV_Report_2026-08-30_08-00-01_123456.html
│   └── ARXIV_Report_2026-08-29_08-00-02_234567.html
└── prl/
    └── PRL_Report_2026-08-30_08-00-01_123456.html
```

**按运行时间（date_grouped，定制新增）**——每次运行一个文件夹，同一批报告在一起：

```text
data/reports/daily_research/html/
├── 2026-08-30_08-00-01_123456/
│   ├── ARXIV_Report.html
│   └── PRL_Report.html
└── 2026-08-29_08-00-02_234567/
    └── ARXIV_Report.html
```

**历史报告兼容**：v3.2 生成的秒级时间戳目录（如 `2026-06-24_08-00-01/`）、v4.0/v4.2 官方布局的报告，在「报告查看」页都能正常浏览；切换布局不影响任何已有报告。

---

## 5. 以后原作者发布新版怎么办

1. 打开你的 GitHub 复刻页面，点 **Sync fork（同步复刻）** 按钮 → 更新 `main` 分支到官方最新版
2. 把 `custom` 分支上的定制提交重新叠加到新版上 —— 这一步涉及命令行操作，**直接把情况发给 AI 助手（如 ZCode）处理**；上游大改（如 4.1 换面板）时需要人工适配
3. 叠加完成后推送到 GitHub，定制就"搬进"新版了

---

## 6. 其他备忘

- **想回到官方原版**：直接用 `main` 分支的代码即可，`custom` 分支放着不影响任何东西。
- **本地仓库位置**：`D:\UsersFiles\Link_Z\Desktop\arxiv-daily-researcher\个人\4.2\`。这是联网的 git 仓库本体，本地改代码 → 提交（commit）→ 推送（push）都发生在这里；GitHub 上的复刻是它的云端备份。
- **personal-info 分支工作副本**：根目录的 `personal-info\` 文件夹是同一仓库 personal-info 分支的检出（git worktree），存放含个人信息的清单等不公开内容，该分支**不要推送到 GitHub**。
- **材料学期刊**：在 WebUI「数据源」页的「额外数据源」区添加 ISSN 期刊来源，不需要改代码（9 本期刊的 ISSN 清单见根目录《服务器版个性化设置清单.md》）。
